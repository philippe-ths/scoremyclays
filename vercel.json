{
  "buildCommand": "cd website && npm run build && cd .. && mkdir -p dist && cp -r website/build/* dist/ && cp index.html dist/index.html && cp robots.txt sitemap.xml dist/",
  "outputDirectory": "dist",
  "installCommand": "cd website && npm install",
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "X-Robots-Tag",
          "value": "noindex, nofollow"
        }
      ]
    }
  ]
}